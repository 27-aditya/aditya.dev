---
import ContentCard from "./ContentCard.astro";

interface Props {
  title?: string;
  heading?: string;
  body?: string;
  image?: string;
}

const { title, heading ,body, image } = Astro.props;
---

<div>
  <div class="flex flex-col border-darkslate-200 bg-darkslate-500 rounded-xl border-2 p-6 hover:border-cyan-300 transition-colors duration-300">
    <div class="flex flex-row">
      <h3 class="p-2 text-xl font-semibold text-white">{heading}</h3>
    </div>
      <div class="text-base leading-relaxed text-gray-200">
      <ContentCard title={title} body={body}>
        <slot />
      </ContentCard>
    </div>
  </div>
</div>
