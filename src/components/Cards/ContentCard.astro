---
interface Props {
  title?: string;
  body?: string;
}

const { title, body } = Astro.props;
---

<>
  {title && <h2 class="text-2xl font-normal px-1 mb-1 z-20 text-white">{title}</h2>}
  {body && <p class="m-1 font-light  text-white">{body}</p>}
  <slot />
</>